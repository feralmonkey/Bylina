-- Called once on startup. You can expand this to load a map, NPCs, dialogues, etc.

-- Spawn the player near the center
--spawn_player(120, 112)

script_file(mapPath + mapName + mapExtension)

-- Later: define interact handlers, area triggers, etc.